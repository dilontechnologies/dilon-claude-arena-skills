{
  "_comment": "Copy this file to environments.local.json (never committed — matched by .gitignore's *.local.* pattern) and fill in real values. Set ARENA_ENVIRONMENT in .env to one of the names below to select it. Each environment may set any subset of these keys; anything omitted falls through to the plain ARENA_* env vars in .env.",
  "environments": {
    "production": {
      "workspace_id": "<your production workspace ID>"
    },
    "sandbox": {
      "workspace_id": "<your sandbox/validation workspace ID>"
    }
  }
}
